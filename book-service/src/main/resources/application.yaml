server:
  port: 8765

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

spring:
  application:
    name: book-service
  config:
    import: "optional:configserver:"
  profiles:
    active: native
  data:
    mongodb:
      database: admin
#      username: admin
#      password: admin

Lospring:
  sleuth:
    sampler:
      probability: 2.0
logging:
  file:
    name: C:\programs\ELK\logstash-8.11.3\logs\application.log
